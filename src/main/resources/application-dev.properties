# Local development defaults. Safe to commit: this DB only exists on a dev machine.
spring.datasource.url=jdbc:postgresql://localhost:5432/borrowly
spring.datasource.username=borrowly
spring.datasource.password=borrowly
