# "cp .env.example .env" at root
# Set to "prod" on the server to run the production profile.
SPRING_PROFILES_ACTIVE=dev
POSTGRES_DB=borrowly
POSTGRES_USER=borrowly
POSTGRES_PASSWORD=borrowly
JWT_SECRET=change-me-to-a-random-secret-at-least-32-bytes-long
JWT_EXPIRATION=86400000
